// SPDX-License-Identifier: CC-BY-SA-4.0
// Copyright (c) Jonathan D.A. Jewell <j.d.a.jewell@open.ac.uk>
= Enaction Engine Architecture Decisions

Accepted decisions are append-only records. Supersede an earlier decision with a
new numbered ADR rather than silently rewriting its outcome.

* `0001` — adopt the RSR standard
* `0002` — variant contract
* `0003` — forge and sustain lifecycle
* `0004` — Rust now, AffineScript later
* `0005` — fixed timestep and render-only interpolation
* `0006` — Enaction Engine identity
* `0007` — cognition, affect, and conation as siblings
* `0008` — complete engine through incremental extraction
* `0009` — UMS integration through versioned contracts
* `0010` — IDApTIK and Chronicles of Slavia proving grounds
* `0011` — repository and crate naming conventions
* `0012` — no speculative empty subsystems
* `0013` — two-layer estate: runtime kernel and authoring studio
* `0014` — two-rate CAC architecture: discrete cognition, continuous affect
* `0015` — kernel determinism contract
* `0016` — `Mass` is the kernel confidence type; an intent read is not one
* `0017` — crate admission and the three-part extraction trigger
* `0018` — the enactive claim
* `0019` — the CAC/ESM contract as types and validators
* `0020` — operation-first coprocessor runtime and deterministic execution lanes
* `0021` — bounded BEAM integration through the estate SNIF boundary
