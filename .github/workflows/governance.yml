# This workflow is managed by gh actions-lock.
# SPDX-License-Identifier: AGPL-3.0-or-later
# This workflow is managed by gh actions-lock.
name: Governance

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
permissions:
  actions: read
  contents: read

jobs:
  governance:
    # Includes the lockfile-aware header-block fix from standards#597.
    uses: hyperpolymath/standards/.github/workflows/governance-reusable.yml@13cb62ace40313527f9576b5d5a6a8f2712356a3
